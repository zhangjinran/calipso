# CALIPSO_Code_new 文档索引

## 项目说明

- `project/REASONIX.md`：项目结构、数据流和运行注意事项
- `project/function_summary.md`：核心函数原理
- `project/CHANGELOG.md`：版本和修改记录

## 计划

统一放在项目根目录的 `plan/` 文件夹中；hdf2mat 目录内的旧计划作为技术历史记录保留。

## 归档

- `archive/backup/`：MATLAB 自动保存文件和旧备份
- `archive/logs/`：旧的调试日志

## 运行结果

统一放在 `result/<run_name>/images`、`blocks`、`logs`。
